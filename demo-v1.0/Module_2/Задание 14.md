 /Настройка selinux
```bash
nano /etc/selinux/config
# Заменив текст SELINUX=enforcing на SELINUX=permissive.
setenforce 0
```
3. Настройка обратного прокси
```bash
dnf install -y nginx
systemctl enable --now nginx

# Дозаписываем в /etc/nginx/nginx.conf перед последней скобкой }
server {
    listen 80;
    server_name web.au-team.irpo;
    location / { 
	    proxy_pass http://172.16.221.2:8080; 
    }
}

server {
    listen 80;
    server_name docker.au-team.irpo;
    location / { proxy_pass http://172.16.222.2:8080; }
}


nginx -t
systemctl restart nginx
```


